<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>ListenTo.me</title>
    <link rel="stylesheet" href="scss/application.css.scss">
    <link href='https://fonts.googleapis.com/css?family=Raleway:400,400italic,700,700italic' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <header id="main_header" class="cf">
      <div class="wrapper_wide">
      <a href="index.html" id="logo">ListenTo.me</a>
      <nav>
        <ul>
          <li><a href="podcasts.html">Find</a></li>
          <li><a href="sign-in.html">Sign In</a></li>
          <li><a href="sign-up.html">Create a Podcast</a></li>
        </ul>
      </nav>
    </div>
    </header>

    <div id="podcast_show">
      <div id="show_banner">
        <div class="wrapper_skinny">
        <h1><%= @podcast.title %></h1>
        <p class="description"><%= @podcast.description %></p>
        <% unless @podcast.itunes.blank? %>
           <a href="<%= @podcast.itunes %>" class="subscription_link">iTunes</a>
        <% end %>
        <% unless @podcast.stitcher.blank? %>
           <a href="<%= @podcast.stitcher %>" class="subscription_link">Stitcher</a>
        <% end %>
        <% unless @podcast.podbay.blank? %>
           <a href="<%= @podcast.podbay %>" class="subscription_link">PodBay</a>
        <% end %>
         </div>
      </div>

      <div id="breadcrumb">
        <%= link_to podcasts_path do %>
          All podcasts
          <span>/</span>
          <%= link_to @podcast.title, podcast_path(@podcast) %>
          <span>/</span>
          <%= @episode.title %>
          <% end %>
      </div>

      <div id="episode_content">
        <div class="wrapper_skinny">
          <%= image_tag @episode.episode_thumbnail.url(:medium), class: "current_episode_thumbnail" %>
          <h2><%= @episode.title %></h2>
          <p class="description"><%= @episode.description %></p>
        </div>
      </div>

      <div id="episodes">
        <div class="wrapper_skinny">
          <ul class="cf">
            <% @episodes.each do |episode| %>
            <li class="cf">
              <div class="episode_thumbnail">
                <a href="#">
                  <img src="images/thisamericanlifeepisode.jpeg" alt="samebedep">
                </a>
              </div>
              <div class="episode_overview">
                <h2><%= link_to episode.title, podcast_episode_path(@podcast, episode)%></h2>
                <p class="description"><%= truncate(episode.description, length: 160)%></p>
              </div>
            </li>
          </ul>
          <% end %>

        </div>
      </div>

      <div class="pagination">
        <span class="previous_page disabled">Previous</span>
        <em class="current">1</em>
        <a href="#">2</a>
        <a class="next_page" href="#">Next</a>
      </div>

    </div>

  </body>
  </html>
